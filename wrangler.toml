name = "gw-wishlist"
main = "src/index.ts"
compatibility_date = "2024-09-25"

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true

# 非機密設定值：直接寫在這裡，確保透過 GitHub 自動部署時一定會生效
# （Dashboard 上手動加的環境變數在這個部署流程下不會套用，只有這個檔案是依據）。
# 日後要新增管理員 email，直接在這裡編輯、commit、push 即可。
[vars]
GOOGLE_CLIENT_ID = "38301055976-3nghd1v00sieg882gvq5vk397sm9gqbt.apps.googleusercontent.com"
ADMIN_EMAILS = "caring841111@gmail.com"

[[d1_databases]]
binding = "DB"
database_name = "gw-wishlist-db"
database_id = "5c968f36-17c1-4da4-abd1-e0bb9eb16e56"
